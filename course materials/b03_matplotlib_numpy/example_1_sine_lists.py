# Creating a sine curve

# Create a list of x values (x_list_floats) from 1 to 20 in increments of 0.01
# Use a list comprehension and a range()

# Now use a list comprehension to create a list of sine values (y_list)


import matplotlib.pyplot as plt

x_list_floats = []
y_list = []

plt.show(x_list_floats, y_list)